@import "../../app.wxss";

.friend-room-page {
  display: flex;
  flex-direction: column;
  gap: 22rpx;
  padding-bottom: calc(28rpx + env(safe-area-inset-bottom));
}

.custom-nav {
  position: sticky;
  top: 0;
  z-index: 20;
  margin: -32rpx -32rpx 0;
  padding-left: 24rpx;
  padding-right: 24rpx;
  background: #f3f7fb;
}

.custom-nav-bar {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.custom-nav-back {
  position: absolute;
  left: 0;
  top: 50%;
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  transform: translateY(-50%);
  min-height: 64rpx;
  padding: 0 12rpx;
  color: #17324d;
}

.custom-nav-back-icon {
  font-size: 36rpx;
  line-height: 1;
}

.custom-nav-back-text,
.custom-nav-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #17324d;
}

.hero-card,
.entry-card,
.state-box,
.preview-card,
.inline-error-card,
.action-card {
  padding: 28rpx;
}

.hero-card {
  background: linear-gradient(145deg, #17324d 0%, #2f6bff 100%);
}

.hero-title {
  display: block;
  font-size: 40rpx;
  line-height: 1.35;
  font-weight: 700;
  color: #ffffff;
}

.hero-copy {
  display: block;
  margin-top: 16rpx;
  font-size: 26rpx;
  line-height: 1.75;
  color: rgba(255, 255, 255, 0.84);
}

.entry-list,
.preview-stack {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.entry-card,
.preview-card {
  background: #ffffff;
}

.entry-title,
.preview-title,
.state-title {
  display: block;
  font-size: 32rpx;
  font-weight: 700;
  color: #17324d;
}

.entry-copy,
.state-text,
.preview-copy,
.inline-error-text {
  display: block;
  margin-top: 12rpx;
  font-size: 24rpx;
  line-height: 1.7;
}

.entry-copy,
.state-text,
.preview-copy {
  color: #5f7596;
}

.invite-input-box {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-top: 18rpx;
}

.invite-input {
  min-height: 88rpx;
  padding: 0 24rpx;
  border-radius: 20rpx;
  background: #f6f9fd;
  border: 1rpx solid rgba(23, 50, 77, 0.08);
  font-size: 30rpx;
  font-weight: 700;
  letter-spacing: 6rpx;
  color: #17324d;
  text-transform: uppercase;
}

.preview-meta {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16rpx;
  margin-top: 22rpx;
}

.preview-meta-item {
  padding: 20rpx;
  border-radius: 20rpx;
  background: #f6f9fd;
}

.preview-meta-label {
  display: block;
  font-size: 22rpx;
  color: #6e84a3;
}

.preview-meta-value {
  display: block;
  margin-top: 10rpx;
  font-size: 28rpx;
  line-height: 1.45;
  font-weight: 700;
  color: #17324d;
  word-break: break-word;
}

.inline-error-text {
  color: #c24343;
}

.action-button {
  min-height: 78rpx;
  margin-top: 18rpx;
}

.action-card .action-button:first-child {
  margin-top: 0;
}

.action-button-disabled {
  opacity: 0.5;
  pointer-events: none;
}

@media (max-width: 420px) {
  .preview-meta {
    grid-template-columns: minmax(0, 1fr);
  }
}
