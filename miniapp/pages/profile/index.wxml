<view class="page-shell">
  <view class="section-card profile-hero">
    <text class="profile-eyebrow">我的主页</text>
    <text class="profile-title">{{displayName}}</text>
    <text class="profile-copy">
      {{isAuthenticated ? '当前阶段已接入最小认证展示、我的学习和统一错题中心入口。' : '请先登录，再进入学习中心和错题中心。'}}
    </text>
    <view wx:if="{{showMockHint && !isAuthenticated}}" class="env-pill">
      仅开发环境可见的测试登录入口
    </view>
  </view>

  <view wx:if="{{!isAuthenticated}}" class="section-card state-box">
    <text class="state-title">你还没有登录</text>
    <text class="state-text">
      登录后会保存最小认证状态，并自动为后续用户接口注入认证头。
    </text>
    <view class="primary-button profile-action" bindtap="handleLogin">去登录</view>
  </view>

  <block wx:else>
    <view class="section-card summary-card">
      <view class="summary-row">
        <text class="summary-label">用户状态</text>
        <text class="summary-value">{{statusText}}</text>
      </view>
      <view class="summary-row">
        <text class="summary-label">经验值</text>
        <text class="summary-value">{{user.experience}}</text>
      </view>
      <view class="summary-row">
        <text class="summary-label">对战分</text>
        <text class="summary-value">{{user.battleRating}}</text>
      </view>
      <view class="summary-row">
        <text class="summary-label">连续学习天数</text>
        <text class="summary-value">{{user.continuousLearningDays}} 天</text>
      </view>
    </view>

    <view class="section-card entry-card">
      <text class="section-title">个人入口</text>
      <text class="section-subtitle">
        我的学习和统一错题中心都已接入正式认证链路。
      </text>
      <view class="entry-list">
        <view class="entry-item" bindtap="handleLearningEntry">
          <text class="entry-title">我的学习</text>
          <text class="entry-copy">查看课程学习记录、筛选进度并继续学习。</text>
        </view>
        <view class="entry-item" bindtap="handleWrongQuestionEntry">
          <text class="entry-title">我的错题</text>
          <text class="entry-copy">查看错题统计、列表和单题详情。</text>
        </view>
      </view>
    </view>

    <view class="section-card action-card">
      <text class="section-title">会话管理</text>
      <text class="section-subtitle">
        退出登录会调用正式接口撤销当前会话，并清理本地认证状态。
      </text>
      <button
        class="logout-button"
        loading="{{isLoggingOut}}"
        disabled="{{isLoggingOut}}"
        bindtap="handleLogout"
      >
        退出登录
      </button>
    </view>
  </block>
</view>
