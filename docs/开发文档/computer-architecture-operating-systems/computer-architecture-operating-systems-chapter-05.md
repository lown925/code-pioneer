# 第五章：存储层次、Cache 与主存

章节简介：理解局部性、Cache 映射、命中与缺失、主存和虚拟地址之前的硬件存储层次。

预计学习时间：120 分钟

章节学习目标：
- 理解并能够应用“存储层次与局部性”的核心规则
- 理解并能够应用“Cache 行与命中”的核心规则
- 理解并能够应用“直接映射与组相联”的核心规则
- 理解并能够应用“Cache 替换与写策略”的核心规则
- 理解并能够应用“主存与内存访问”的核心规则
- 理解并能够应用“AMAT 与存储性能分析”的核心规则

---

## 课时 1：存储层次与局部性

课时简介：寄存器、Cache、主存和外存形成容量、速度、成本不同的层次，局部性使缓存成为可能。

预计学习时间：20 分钟

### 正文

[标题]
存储层次与局部性：先看系统状态，再看机制

[文本]
寄存器、Cache、主存和外存形成容量、速度、成本不同的层次，局部性使缓存成为可能。 学习这一课时，重点不是孤立背名词，而是明确“谁保存状态、谁改变状态、变化发生在哪个层次”。这样才能把硬件行为和操作系统抽象联系起来。

[代码 language=python]
# 用伪代码表达本课的状态关系，不依赖具体运行环境
recent_addresses.append(address)
[/代码]

[标题]
边界与工程判断

[文本]
时间和空间局部性是缓存有效的重要原因。在分析题或真实故障中，应先固定初始条件，再沿数据路径或控制路径逐步推导；不能因为某个部件“通常更快”或“通常这样实现”，就把实现习惯误当成体系结构保证。

[示例 title=存储层次与局部性分析示例]
说明：假设系统状态已经给定，先判断当前层次负责什么，再判断下一步状态变化。
语言：text
输入状态 -> 当前机制 -> 状态变化 -> 对上层可见结果
[/示例]

[提示 title=存储层次与局部性学习提示]
更大容量通常伴随不同的访问成本。做题时把“定义”“机制”“性能影响”分开判断，通常能排除绝对化选项。

[警告 title=不要混淆抽象与具体实现]
课程中的 Python 片段仅用于表达算法或状态转换，不代表底层系统由 Python 实现。不同 CPU、操作系统和文件系统可以采用不同实现，只要满足相应接口与语义。

### 课时题目

#### 题目 73

题型：SINGLE_CHOICE
题干：学习“存储层次与局部性”时，下列哪项判断最准确？
难度：EASY
分值：10
知识点：存储层次与局部性
是否用于 Battle：否

选项：
- A. 更大容量通常伴随不同的访问成本 [正确]
- B. 只要提高 CPU 主频，就能消除这一机制中的所有性能问题
- C. 操作系统抽象与硬件状态完全无关
- D. 具体实现习惯永远等同于体系结构规定

解析：
本课的核心是“更大容量通常伴随不同的访问成本”。系统问题需要区分接口语义、内部机制与性能结果，不能用绝对化结论替代条件分析。

#### 题目 74

题型：SINGLE_CHOICE
题干：在“存储层次与局部性”场景中，已知系统初始状态固定。为了得到可靠结论，下一步最合理的分析方式是什么？
难度：MEDIUM
分值：10
知识点：存储层次与局部性、系统状态分析
是否用于 Battle：是
Battle 难度：MEDIUM
Battle 展示类型：执行流程

选项：
- A. 沿相关数据路径或控制路径跟踪状态变化，并检查“时间和空间局部性是缓存有效的重要原因” [正确]
- B. 忽略初始状态，只根据术语名称猜测结果
- C. 默认所有实现的延迟、容量和策略完全相同
- D. 只看最终输出，不检查中间状态是否满足约束

解析：
系统级推理依赖初始状态和状态转移。正确做法是沿实际机制逐步推导，并检查本课的重要边界：时间和空间局部性是缓存有效的重要原因。

#### 题目 75

题型：SINGLE_CHOICE
题干：某同学分析“存储层次与局部性”时直接得出“只要采用更复杂的实现，正确性和性能一定同时提高”。为什么这个结论不可靠？
难度：HARD
分值：10
知识点：存储层次与局部性、边界分析
是否用于 Battle：是
Battle 难度：HARD
Battle 展示类型：场景判断

选项：
- A. 正确性必须先满足体系结构或操作系统语义，性能还取决于工作负载与实现代价 [正确]
- B. 因为复杂实现一定比简单实现慢
- C. 因为硬件机制不会影响软件执行
- D. 因为所有系统都必须采用完全相同的内部结构

解析：
系统设计首先要保持可见语义正确，再讨论特定工作负载下的性能收益和成本。“更大容量通常伴随不同的访问成本”不能被简化成无条件的性能承诺。


---

## 课时 2：Cache 行与命中

课时简介：Cache 以块或行搬运数据；访问地址对应数据已在 Cache 中时称为命中。

预计学习时间：20 分钟

### 正文

[标题]
Cache 行与命中：先看系统状态，再看机制

[文本]
Cache 以块或行搬运数据；访问地址对应数据已在 Cache 中时称为命中。 学习这一课时，重点不是孤立背名词，而是明确“谁保存状态、谁改变状态、变化发生在哪个层次”。这样才能把硬件行为和操作系统抽象联系起来。

[代码 language=python]
# 用伪代码表达本课的状态关系，不依赖具体运行环境
hit = tag_match and valid
[/代码]

[标题]
边界与工程判断

[文本]
Cache 命中避免了更慢层次的完整访问延迟。在分析题或真实故障中，应先固定初始条件，再沿数据路径或控制路径逐步推导；不能因为某个部件“通常更快”或“通常这样实现”，就把实现习惯误当成体系结构保证。

[示例 title=Cache 行与命中分析示例]
说明：假设系统状态已经给定，先判断当前层次负责什么，再判断下一步状态变化。
语言：text
输入状态 -> 当前机制 -> 状态变化 -> 对上层可见结果
[/示例]

[提示 title=Cache 行与命中学习提示]
缓存管理粒度通常大于单个字节。做题时把“定义”“机制”“性能影响”分开判断，通常能排除绝对化选项。

[警告 title=不要混淆抽象与具体实现]
课程中的 Python 片段仅用于表达算法或状态转换，不代表底层系统由 Python 实现。不同 CPU、操作系统和文件系统可以采用不同实现，只要满足相应接口与语义。

### 课时题目

#### 题目 76

题型：SINGLE_CHOICE
题干：学习“Cache 行与命中”时，下列哪项判断最准确？
难度：EASY
分值：10
知识点：Cache 行与命中
是否用于 Battle：否

选项：
- A. 缓存管理粒度通常大于单个字节 [正确]
- B. 只要提高 CPU 主频，就能消除这一机制中的所有性能问题
- C. 操作系统抽象与硬件状态完全无关
- D. 具体实现习惯永远等同于体系结构规定

解析：
本课的核心是“缓存管理粒度通常大于单个字节”。系统问题需要区分接口语义、内部机制与性能结果，不能用绝对化结论替代条件分析。

#### 题目 77

题型：SINGLE_CHOICE
题干：在“Cache 行与命中”场景中，已知系统初始状态固定。为了得到可靠结论，下一步最合理的分析方式是什么？
难度：MEDIUM
分值：10
知识点：Cache 行与命中、系统状态分析
是否用于 Battle：是
Battle 难度：MEDIUM
Battle 展示类型：执行流程

选项：
- A. 沿相关数据路径或控制路径跟踪状态变化，并检查“Cache 命中避免了更慢层次的完整访问延迟” [正确]
- B. 忽略初始状态，只根据术语名称猜测结果
- C. 默认所有实现的延迟、容量和策略完全相同
- D. 只看最终输出，不检查中间状态是否满足约束

解析：
系统级推理依赖初始状态和状态转移。正确做法是沿实际机制逐步推导，并检查本课的重要边界：Cache 命中避免了更慢层次的完整访问延迟。

#### 题目 78

题型：CODE_FILL
题干：补全下面用于表达“Cache 行与命中”核心机制的伪代码。请填写 `____` 缺失部分，使状态关系成立。
难度：HARD
分值：10
知识点：Cache 行与命中、状态推导
是否用于 Battle：是
Battle 难度：HARD
Battle 展示类型：代码填空

题目代码:
```python
hit = tag_match and ____
```

可接受答案:
```text
valid
```

判题设置：
- 区分大小写：是
- 忽略首尾空格：是
- 保留代码内部空格：否
- 统一 Windows 和 Unix 换行：是

解析：
缺失部分应为 `valid`。补全后表达了本课的关键状态关系；代码只是机制模型，重点是理解状态如何变化。

标准完整代码:
```python
hit = tag_match and valid
```


---

## 课时 3：直接映射与组相联

课时简介：映射策略决定一个内存块可以放入哪些 Cache 位置，关联度影响冲突缺失与硬件复杂度。

预计学习时间：20 分钟

### 正文

[标题]
直接映射与组相联：先看系统状态，再看机制

[文本]
映射策略决定一个内存块可以放入哪些 Cache 位置，关联度影响冲突缺失与硬件复杂度。 学习这一课时，重点不是孤立背名词，而是明确“谁保存状态、谁改变状态、变化发生在哪个层次”。这样才能把硬件行为和操作系统抽象联系起来。

[代码 language=python]
# 用伪代码表达本课的状态关系，不依赖具体运行环境
set_index = block_number % set_count
[/代码]

[标题]
边界与工程判断

[文本]
提高关联度通常能减少部分冲突缺失。在分析题或真实故障中，应先固定初始条件，再沿数据路径或控制路径逐步推导；不能因为某个部件“通常更快”或“通常这样实现”，就把实现习惯误当成体系结构保证。

[示例 title=直接映射与组相联分析示例]
说明：假设系统状态已经给定，先判断当前层次负责什么，再判断下一步状态变化。
语言：text
输入状态 -> 当前机制 -> 状态变化 -> 对上层可见结果
[/示例]

[提示 title=直接映射与组相联学习提示]
映射方式不会改变程序可见数据值。做题时把“定义”“机制”“性能影响”分开判断，通常能排除绝对化选项。

[警告 title=不要混淆抽象与具体实现]
课程中的 Python 片段仅用于表达算法或状态转换，不代表底层系统由 Python 实现。不同 CPU、操作系统和文件系统可以采用不同实现，只要满足相应接口与语义。

### 课时题目

#### 题目 79

题型：SINGLE_CHOICE
题干：学习“直接映射与组相联”时，下列哪项判断最准确？
难度：EASY
分值：10
知识点：直接映射与组相联
是否用于 Battle：否

选项：
- A. 映射方式不会改变程序可见数据值 [正确]
- B. 只要提高 CPU 主频，就能消除这一机制中的所有性能问题
- C. 操作系统抽象与硬件状态完全无关
- D. 具体实现习惯永远等同于体系结构规定

解析：
本课的核心是“映射方式不会改变程序可见数据值”。系统问题需要区分接口语义、内部机制与性能结果，不能用绝对化结论替代条件分析。

#### 题目 80

题型：SINGLE_CHOICE
题干：在“直接映射与组相联”场景中，已知系统初始状态固定。为了得到可靠结论，下一步最合理的分析方式是什么？
难度：MEDIUM
分值：10
知识点：直接映射与组相联、系统状态分析
是否用于 Battle：是
Battle 难度：MEDIUM
Battle 展示类型：执行流程

选项：
- A. 沿相关数据路径或控制路径跟踪状态变化，并检查“提高关联度通常能减少部分冲突缺失” [正确]
- B. 忽略初始状态，只根据术语名称猜测结果
- C. 默认所有实现的延迟、容量和策略完全相同
- D. 只看最终输出，不检查中间状态是否满足约束

解析：
系统级推理依赖初始状态和状态转移。正确做法是沿实际机制逐步推导，并检查本课的重要边界：提高关联度通常能减少部分冲突缺失。

#### 题目 81

题型：SINGLE_CHOICE
题干：某同学分析“直接映射与组相联”时直接得出“只要采用更复杂的实现，正确性和性能一定同时提高”。为什么这个结论不可靠？
难度：HARD
分值：10
知识点：直接映射与组相联、边界分析
是否用于 Battle：是
Battle 难度：HARD
Battle 展示类型：场景判断

选项：
- A. 正确性必须先满足体系结构或操作系统语义，性能还取决于工作负载与实现代价 [正确]
- B. 因为复杂实现一定比简单实现慢
- C. 因为硬件机制不会影响软件执行
- D. 因为所有系统都必须采用完全相同的内部结构

解析：
系统设计首先要保持可见语义正确，再讨论特定工作负载下的性能收益和成本。“映射方式不会改变程序可见数据值”不能被简化成无条件的性能承诺。


---

## 课时 4：Cache 替换与写策略

课时简介：替换策略选择被淘汰的 Cache 行；写直达与写回决定修改何时传播到下一层。

预计学习时间：20 分钟

### 正文

[标题]
Cache 替换与写策略：先看系统状态，再看机制

[文本]
替换策略选择被淘汰的 Cache 行；写直达与写回决定修改何时传播到下一层。 学习这一课时，重点不是孤立背名词，而是明确“谁保存状态、谁改变状态、变化发生在哪个层次”。这样才能把硬件行为和操作系统抽象联系起来。

[代码 language=python]
# 用伪代码表达本课的状态关系，不依赖具体运行环境
line.dirty = True  # 写回缓存发生修改
[/代码]

[标题]
边界与工程判断

[文本]
写回策略需要跟踪脏状态。在分析题或真实故障中，应先固定初始条件，再沿数据路径或控制路径逐步推导；不能因为某个部件“通常更快”或“通常这样实现”，就把实现习惯误当成体系结构保证。

[示例 title=Cache 替换与写策略分析示例]
说明：假设系统状态已经给定，先判断当前层次负责什么，再判断下一步状态变化。
语言：text
输入状态 -> 当前机制 -> 状态变化 -> 对上层可见结果
[/示例]

[提示 title=Cache 替换与写策略学习提示]
替换和写策略会影响性能与一致性实现。做题时把“定义”“机制”“性能影响”分开判断，通常能排除绝对化选项。

[警告 title=不要混淆抽象与具体实现]
课程中的 Python 片段仅用于表达算法或状态转换，不代表底层系统由 Python 实现。不同 CPU、操作系统和文件系统可以采用不同实现，只要满足相应接口与语义。

### 课时题目

#### 题目 82

题型：SINGLE_CHOICE
题干：学习“Cache 替换与写策略”时，下列哪项判断最准确？
难度：EASY
分值：10
知识点：Cache 替换与写策略
是否用于 Battle：否

选项：
- A. 替换和写策略会影响性能与一致性实现 [正确]
- B. 只要提高 CPU 主频，就能消除这一机制中的所有性能问题
- C. 操作系统抽象与硬件状态完全无关
- D. 具体实现习惯永远等同于体系结构规定

解析：
本课的核心是“替换和写策略会影响性能与一致性实现”。系统问题需要区分接口语义、内部机制与性能结果，不能用绝对化结论替代条件分析。

#### 题目 83

题型：SINGLE_CHOICE
题干：在“Cache 替换与写策略”场景中，已知系统初始状态固定。为了得到可靠结论，下一步最合理的分析方式是什么？
难度：MEDIUM
分值：10
知识点：Cache 替换与写策略、系统状态分析
是否用于 Battle：是
Battle 难度：MEDIUM
Battle 展示类型：执行流程

选项：
- A. 沿相关数据路径或控制路径跟踪状态变化，并检查“写回策略需要跟踪脏状态” [正确]
- B. 忽略初始状态，只根据术语名称猜测结果
- C. 默认所有实现的延迟、容量和策略完全相同
- D. 只看最终输出，不检查中间状态是否满足约束

解析：
系统级推理依赖初始状态和状态转移。正确做法是沿实际机制逐步推导，并检查本课的重要边界：写回策略需要跟踪脏状态。

#### 题目 84

题型：CODE_FILL
题干：补全下面用于表达“Cache 替换与写策略”核心机制的伪代码。请填写 `____` 缺失部分，使状态关系成立。
难度：HARD
分值：10
知识点：Cache 替换与写策略、状态推导
是否用于 Battle：是
Battle 难度：HARD
Battle 展示类型：代码填空

题目代码:
```python
line.dirty = ____  # 写回缓存发生修改
```

可接受答案:
```text
True
```

判题设置：
- 区分大小写：是
- 忽略首尾空格：是
- 保留代码内部空格：否
- 统一 Windows 和 Unix 换行：是

解析：
缺失部分应为 `True`。补全后表达了本课的关键状态关系；代码只是机制模型，重点是理解状态如何变化。

标准完整代码:
```python
line.dirty = True  # 写回缓存发生修改
```


---

## 课时 5：主存与内存访问

课时简介：主存容量大于 Cache 但延迟更高，控制器负责组织读写请求和时序。

预计学习时间：20 分钟

### 正文

[标题]
主存与内存访问：先看系统状态，再看机制

[文本]
主存容量大于 Cache 但延迟更高，控制器负责组织读写请求和时序。 学习这一课时，重点不是孤立背名词，而是明确“谁保存状态、谁改变状态、变化发生在哪个层次”。这样才能把硬件行为和操作系统抽象联系起来。

[代码 language=python]
# 用伪代码表达本课的状态关系，不依赖具体运行环境
physical_address = row_base + column_offset
[/代码]

[标题]
边界与工程判断

[文本]
CPU 访问主存通常经过缓存和内存控制路径。在分析题或真实故障中，应先固定初始条件，再沿数据路径或控制路径逐步推导；不能因为某个部件“通常更快”或“通常这样实现”，就把实现习惯误当成体系结构保证。

[示例 title=主存与内存访问分析示例]
说明：假设系统状态已经给定，先判断当前层次负责什么，再判断下一步状态变化。
语言：text
输入状态 -> 当前机制 -> 状态变化 -> 对上层可见结果
[/示例]

[提示 title=主存与内存访问学习提示]
主存不是处理器内部寄存器。做题时把“定义”“机制”“性能影响”分开判断，通常能排除绝对化选项。

[警告 title=不要混淆抽象与具体实现]
课程中的 Python 片段仅用于表达算法或状态转换，不代表底层系统由 Python 实现。不同 CPU、操作系统和文件系统可以采用不同实现，只要满足相应接口与语义。

### 课时题目

#### 题目 85

题型：SINGLE_CHOICE
题干：学习“主存与内存访问”时，下列哪项判断最准确？
难度：EASY
分值：10
知识点：主存与内存访问
是否用于 Battle：否

选项：
- A. 主存不是处理器内部寄存器 [正确]
- B. 只要提高 CPU 主频，就能消除这一机制中的所有性能问题
- C. 操作系统抽象与硬件状态完全无关
- D. 具体实现习惯永远等同于体系结构规定

解析：
本课的核心是“主存不是处理器内部寄存器”。系统问题需要区分接口语义、内部机制与性能结果，不能用绝对化结论替代条件分析。

#### 题目 86

题型：SINGLE_CHOICE
题干：在“主存与内存访问”场景中，已知系统初始状态固定。为了得到可靠结论，下一步最合理的分析方式是什么？
难度：MEDIUM
分值：10
知识点：主存与内存访问、系统状态分析
是否用于 Battle：是
Battle 难度：MEDIUM
Battle 展示类型：执行流程

选项：
- A. 沿相关数据路径或控制路径跟踪状态变化，并检查“CPU 访问主存通常经过缓存和内存控制路径” [正确]
- B. 忽略初始状态，只根据术语名称猜测结果
- C. 默认所有实现的延迟、容量和策略完全相同
- D. 只看最终输出，不检查中间状态是否满足约束

解析：
系统级推理依赖初始状态和状态转移。正确做法是沿实际机制逐步推导，并检查本课的重要边界：CPU 访问主存通常经过缓存和内存控制路径。

#### 题目 87

题型：SINGLE_CHOICE
题干：某同学分析“主存与内存访问”时直接得出“只要采用更复杂的实现，正确性和性能一定同时提高”。为什么这个结论不可靠？
难度：HARD
分值：10
知识点：主存与内存访问、边界分析
是否用于 Battle：是
Battle 难度：HARD
Battle 展示类型：场景判断

选项：
- A. 正确性必须先满足体系结构或操作系统语义，性能还取决于工作负载与实现代价 [正确]
- B. 因为复杂实现一定比简单实现慢
- C. 因为硬件机制不会影响软件执行
- D. 因为所有系统都必须采用完全相同的内部结构

解析：
系统设计首先要保持可见语义正确，再讨论特定工作负载下的性能收益和成本。“主存不是处理器内部寄存器”不能被简化成无条件的性能承诺。


---

## 课时 6：AMAT 与存储性能分析

课时简介：平均内存访问时间可结合命中时间、缺失率和缺失代价估算，用于定位存储瓶颈。

预计学习时间：20 分钟

### 正文

[标题]
AMAT 与存储性能分析：先看系统状态，再看机制

[文本]
平均内存访问时间可结合命中时间、缺失率和缺失代价估算，用于定位存储瓶颈。 学习这一课时，重点不是孤立背名词，而是明确“谁保存状态、谁改变状态、变化发生在哪个层次”。这样才能把硬件行为和操作系统抽象联系起来。

[代码 language=python]
# 用伪代码表达本课的状态关系，不依赖具体运行环境
amat = hit_time + miss_rate * miss_penalty
[/代码]

[标题]
边界与工程判断

[文本]
低缺失率仍可能因高缺失代价影响性能。在分析题或真实故障中，应先固定初始条件，再沿数据路径或控制路径逐步推导；不能因为某个部件“通常更快”或“通常这样实现”，就把实现习惯误当成体系结构保证。

[示例 title=AMAT 与存储性能分析分析示例]
说明：假设系统状态已经给定，先判断当前层次负责什么，再判断下一步状态变化。
语言：text
输入状态 -> 当前机制 -> 状态变化 -> 对上层可见结果
[/示例]

[提示 title=AMAT 与存储性能分析学习提示]
性能分析要同时看概率和代价。做题时把“定义”“机制”“性能影响”分开判断，通常能排除绝对化选项。

[警告 title=不要混淆抽象与具体实现]
课程中的 Python 片段仅用于表达算法或状态转换，不代表底层系统由 Python 实现。不同 CPU、操作系统和文件系统可以采用不同实现，只要满足相应接口与语义。

### 课时题目

#### 题目 88

题型：SINGLE_CHOICE
题干：学习“AMAT 与存储性能分析”时，下列哪项判断最准确？
难度：EASY
分值：10
知识点：AMAT 与存储性能分析
是否用于 Battle：否

选项：
- A. 性能分析要同时看概率和代价 [正确]
- B. 只要提高 CPU 主频，就能消除这一机制中的所有性能问题
- C. 操作系统抽象与硬件状态完全无关
- D. 具体实现习惯永远等同于体系结构规定

解析：
本课的核心是“性能分析要同时看概率和代价”。系统问题需要区分接口语义、内部机制与性能结果，不能用绝对化结论替代条件分析。

#### 题目 89

题型：SINGLE_CHOICE
题干：在“AMAT 与存储性能分析”场景中，已知系统初始状态固定。为了得到可靠结论，下一步最合理的分析方式是什么？
难度：MEDIUM
分值：10
知识点：AMAT 与存储性能分析、系统状态分析
是否用于 Battle：是
Battle 难度：MEDIUM
Battle 展示类型：执行流程

选项：
- A. 沿相关数据路径或控制路径跟踪状态变化，并检查“低缺失率仍可能因高缺失代价影响性能” [正确]
- B. 忽略初始状态，只根据术语名称猜测结果
- C. 默认所有实现的延迟、容量和策略完全相同
- D. 只看最终输出，不检查中间状态是否满足约束

解析：
系统级推理依赖初始状态和状态转移。正确做法是沿实际机制逐步推导，并检查本课的重要边界：低缺失率仍可能因高缺失代价影响性能。

#### 题目 90

题型：CODE_FILL
题干：补全下面用于表达“AMAT 与存储性能分析”核心机制的伪代码。请填写 `____` 缺失部分，使状态关系成立。
难度：HARD
分值：10
知识点：AMAT 与存储性能分析、状态推导
是否用于 Battle：是
Battle 难度：HARD
Battle 展示类型：代码填空

题目代码:
```python
amat = hit_time + miss_rate * ____
```

可接受答案:
```text
miss_penalty
```

判题设置：
- 区分大小写：是
- 忽略首尾空格：是
- 保留代码内部空格：否
- 统一 Windows 和 Unix 换行：是

解析：
缺失部分应为 `miss_penalty`。补全后表达了本课的关键状态关系；代码只是机制模型，重点是理解状态如何变化。

标准完整代码:
```python
amat = hit_time + miss_rate * miss_penalty
```


---

